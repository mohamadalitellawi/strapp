# Streamlit Community Cloud reads this file to know which libraries to install.
# We keep it simple and in step with pyproject.toml.
streamlit>=1.58.0
numpy>=2.4.6
pandas>=3.0.3
matplotlib>=3.10.9
plotly>=6.7.0
handcalcs>=1.11.0
